friendly_name: Away Mode
icon_template: >-
  {% if is_state('person.zaven', 'Work') %}
    mdi:office-building
  {% else %}
    mdi:car
  {% endif %}
value_template: >-
  {{ is_state('input_select.comfort_setting', 'Away')}}
turn_on:
  service: script.away_mode
turn_off:
  service: script.away_mode